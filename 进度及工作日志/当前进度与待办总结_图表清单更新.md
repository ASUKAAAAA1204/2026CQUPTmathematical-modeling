# 当前进度与待办总结（按图表清单更新）

## 1. 目的

本文档用于同步四件事：

1. 当前整体进度
2. 问题四当前完成度
3. 待办事项
4. 按既定图表清单更新图表状态

本文件应与以下文档配套使用：

- `../赛题主要文件夹/图表需求与数据来源说明.md`
- `../赛题主要文件夹/结果输出/最新结果索引.md`
- `../赛题主要文件夹/结果输出/figures/README.md`

---

## 2. 当前整体进度总结

## 2.0 最新更新

已完成：

- 图表需求与数据来源说明已改成执行版
- `结果输出/figures/README.md` 已同步图表口径

当前状态：

- 图表层级：`清单已更新，等待关键图同步刷新`
- 论文层级：`可按最新图表口径继续嵌入`
- 代码层级：`问题1-4主流程可跑通，问题4仍需补正式灰区规则`

## 2.1 已完成模块

### 问题一

已完成：

- 第 0 天初值统计
- 初值与长期保留率相关性检验
- 共享平均衰减函数与个体独立拟合比较
- 第 23-29 天统一模型预测
- LOSO 交叉验证输出

当前状态：`基本完成`

### 问题二

已完成：

- 主效应与交互效应回归
- 聚类稳健标准误
- VIF 诊断
- 共线性诊断
- 岭回归稳健性
- 收缩模型对比
- 消融分析
- 锈蚀钢筋重点窗口分析
- 交叉验证输出

当前状态：`结果已成型，论文口径需同步最新版`

### 问题三

已完成：

- 1-90 天动态阈值修正表
- 关键天数阈值表
- 固定阈值对照表
- 单调性校验
- 阈值 bootstrap 区间
- 阈值主表单调包络修正

当前状态：`核心完成`

注意：

- 正式旧文件与 `_retry1` 文件并存
- 单调性校验应以最新版 `_retry1` 为准

### 问题四

已完成：

- 反推初值计算
- 基于 `R_s(t)` 的动态判定逻辑
- cluster block bootstrap
- 三区域判定
- 辅助概率输出
- 问题四总结果表输出
- 动态阈值列
- 区间相对宽度列
- 高风险提示列
- bootstrap 次数已提升到 `1000`

当前状态：`主流程完成，工程化输入接口未完成`

---

## 2.2 当前代码层真实完成度

以 [solve_b.py](../赛题主要文件夹/结果输出/solve_b.py) 为准：

已落实：

- 问题1-4主脚本可跑通
- 结果输出带锁文件回退机制
- 最新结果索引已建立

未完全落实：

- `docx` 报告生成环境仍异常
- 正式文件名与 `_retry1` 文件尚未统一
- 问题四真实现场输入接口尚未接入
- 问题四“区间相对宽度超阈值自动灰区”尚未作为正式主判定规则写入

---

## 3. 问题四当前进度总结

## 3.1 已完成内容

问题四当前已经不是方案阶段，而是已有可运行结果：

- 输出文件：`问题4_三区域判定结果_retry1.csv`
- 字段包括：
  - 样品类型
  - 样品编号
  - 测量天数
  - 观测剩磁
  - 保留率
  - 反推初值点估计
  - 反推初值区间
  - 三区域判定
  - 辅助概率

这意味着：

- 题目规定动作已经可答
- 论文中问题四小节已经有数据支撑
- 图表 7、图表 8 已经有底层数据和流程逻辑

## 3.2 还未完成内容

问题四距离“最终强版”还差以下几点：

1. 将“相对宽度 > 阈值”写入正式自动灰区规则
2. 增加真实案件输入接口
3. 增加外部天气轨迹读入
4. 增加测量误差与重复测量输入
5. 将问题四正文完全同步到最新版代码输出

---

## 4. 待办事项总结

## 4.1 一级待办：必须完成

1. 统一正式文件名与 `_retry1` 最新文件
2. 用最新版结果重新生成论文引用表
3. 重跑图表脚本，确保图 4 到图 8 与最新版结果一致
4. 把问题四 bootstrap 次数从 `200` 提升到 `1000`
5. 在问题四代码中加入“区间相对宽度超阈值 -> 灰区”规则
6. 让问题三、问题四正文完全对齐最新版输出

## 4.2 二级待办：提升论文强度

1. 补充问题四外部数据说明
2. 补充问题四代码输入字段文档
3. 在论文中加入“真实工程使用条件”说明
4. 在答辩口径中加入“灰区复检建议”

## 4.3 三级待办：工程扩展

1. 增加 JSON/Excel 现场输入接口
2. 增加外部天气数据接入
3. 支持批量案件判定
4. 支持误差传播与重复测量修正

---

## 5. 按图表清单更新状态

以下状态以 `图表需求与数据来源说明.md` 的 8 张必做图为准。

| 图号 | 图表名称 | 当前文件 | 当前状态 | 是否建议重生成 | 备注 |
|---|---|---|---|---|---|
| 图1 | 四类样品原始剩磁衰减曲线 | `fig1_raw_decay.svg` | 已生成 | 否 | 基础原始数据未变 |
| 图2 | 四类样品归一化曲线叠加图 | `fig2_normalized_overlay.svg` | 已生成 | 否 | 结构发现图，仍可用 |
| 图3 | `M0` 与 `M90/M0` 散点图 | `fig3_m0_vs_m90_ratio.svg` | 已生成 | 否 | 与问题一当前口径一致 |
| 图4 | 问题2系数图 | `fig4_problem2_coefficients.svg` | 已生成 | 建议是 | 建议用最新版 `_retry1` 结果刷新 |
| 图5 | 问题2消融对比图 | `fig5_problem2_ablation.svg` | 已生成 | 建议是 | 建议与最新版问题2输出同步 |
| 图6 | 1-90 天动态阈值曲线图 | `fig6_dynamic_thresholds.svg` | 已生成 | 必须是 | 问题三主表已更新，应重生成 |
| 图7 | 区间宽度-延迟天数曲线图 | `fig7_interval_width_delay.svg` | 已生成 | 必须是 | 问题四最新版应以 `_retry1` 数据刷新 |
| 图8 | 现场判定流程图 | `fig8_flowchart.svg` | 已生成 | 建议是 | 需与最新问题四术语同步 |

---

## 6. 图表层结论

从“有没有图”角度看：

- 8 张必做图已经全部存在

从“能不能直接交论文”角度看：

- 图1、图2、图3：基本可直接用
- 图4、图5：建议随问题二最新版数据重生成
- 图6、图7、图8：必须与最新版问题三、问题四结果再同步一次

因此，图表工作不是“从零开始画”，而是“对关键图做最新版同步刷新”。

## 6.1 附录补充图状态

当前建议补充的附录图包括：

- 残差诊断图
- 预测值 vs 观测值图
- 系数森林图
- 交叉验证误差分布图
- Bootstrap 区间覆盖图

当前状态：

- 这些图属于增强项，不影响正文主线闭环
- 如果时间充足，优先按问题2和问题4的最新版输出生成
- 若时间紧张，可先保留为答辩备用材料

---

## 7. 建议的下一步推进顺序

1. 先统一使用 `最新结果索引.md`
2. 重生成图4-图8
3. 更新论文中问题二、问题三、问题四对应图注
4. 把问题四灰区规则补进代码
5. 统一正式文件名与 `_retry1` 最新文件
6. 最后处理 `docx` 报告环境
7. 继续同步三人分工中的论文、代码、图表任务边界

---

## 8. 对三人分工的直接影响

### A 建模负责人

- 锁定问题四最终判定规则
- 确认灰区阈值口径
- 确认外部数据最小集合

### B 代码与实验负责人

- 修改问题四 bootstrap 次数
- 增加相对宽度字段与灰区规则
- 重生成图4-图8
- 统一最新版输出

### C 论文与图表负责人

- 更新问题四正式章节
- 更新图6-图8图注与正文引用
- 按需补充附录图清单与图注
- 答辩中统一“工程判定”口径

---

## 9. 当前一句话判断

当前项目已经从“建模方案阶段”进入“结果同步与成稿打磨阶段”。  
问题四主模型和主输出已经具备，但若要达到最终提交强度，必须继续完成：

- 代码规则补齐
- 图表刷新
- 论文同步
- 文件版本统一
