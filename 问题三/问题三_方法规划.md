# 问题三方法规划

## 1. 目标

问题三的目标不是“把阈值改小”，而是把国标静态阈值映射到给定延迟天数和天气轨迹下的观测时点，形成可查表、可解释、可答辩的动态判定线。

核心表达：

```text
Theta_s(t) = Theta_s^GB * R_s(t)
M0_hat = M_obs / R_s(t)
```

两者等价，前者适合出表，后者适合解释。

---

## 2. 现有状态

当前代码已经完成：

- 1-90 天动态阈值 Excel 表
- 四类样品对应的保留率映射
- 类型均值预测剩磁

但还缺两点：

1. 正文口径还需要进一步统一为“时点等效观测判定线”
2. 输出结果还可以再补一个简表，方便论文直接引用关键天数

---

## 3. 推荐最终口径

正文建议统一写成：

> 本文不改变国标阈值本身，而是将其通过类型保留率函数映射到现场观测时点，得到时点等效观测判定线。

并明确：

- 不写“阈值衰减”
- 不写“阈值变小”
- 不写“阈值随时间降低”

只写：

- `动态判定阈值`
- `时点等效观测判定线`
- `反推初值判定`

---

## 4. 方法设计

### 4.1 输入

- 样品类型
- 延迟天数
- 天气轨迹
- 类型保留率函数 `R_s(t)`

### 4.2 输出

- 该时点的等效观测阈值 `Theta_s(t)`
- 该时点的类型预测剩磁
- 可选：反推初值下的判断结果

### 4.3 判定逻辑

```text
若 M_obs >= Theta_s(t)，则观测值满足该时点要求
若 M0_hat >= Theta_s^GB，则等价地满足国标初值要求
```

---

## 5. 建议优化

### 5.1 输出文件优化

建议在现有 Excel 之外，再补一个简表：

- `问题3_阈值关键点.csv`

包含关键天数：

- 1
- 7
- 15
- 22
- 30
- 58
- 90

列建议：

- 天数
- 天气
- 温度
- 湿度
- 四类样品动态阈值
- 四类样品预测剩磁
- 四类样品阈值裕度

这样论文中更容易直接引用。

### 5.2 代码口径优化

建议在代码里统一两个命名：

- `theta_obs`：时点等效观测判定线
- `m0_hat`：反推初值

并在报告里固定一句解释：

> 动态阈值本质上是国标阈值经由保留率函数映射后的观测时点表达。

### 5.3 论文表达优化

建议把问题三写成三段：

1. 定义动态判定线
2. 给出 1-90 天表
3. 用反推初值解释其等价性

---

## 6. 代码层建议

后续可补两项：

1. 生成 `问题3_阈值关键点.csv`
2. 在 Markdown 报告中增加问题三单独小节，写清“表格如何解释”

---

## 7. 风险边界

需要避免的错误写法：

- 把动态阈值当成新的国标
- 把反推初值当成独立新模型
- 把等效映射写成强因果机制

正确定位是：

> 工程映射 + 判定辅助 + 可查表输出
