# B题统一建模方案（竞赛提交版）

## 1. 总体路线

本次修订不再继续拔高模型概念，而是回到题目四问本身，形成一套完成度高、统计规范、工程清楚、带适度创新的竞赛建模方案。

总原则如下：

1. 保留真正有价值的结构发现。
2. 删除数据支撑不足的强机理包装。
3. 四问逐条作答，不用自造框架替代题目要求。
4. 用规范统计结果把创新点钉实。
5. 保证文档、公式、程序一致。

一句话概括：

> 本文基于“个体初值 × 类型平均衰减函数”的乘法结构，建立剩磁衰减经验模型；在此基础上完成影响因素统计分析、动态阈值修正表构造以及基于 block bootstrap 的三区域工程判定流程。

---

## 2. 必须保留的主线

以下内容保留不动：

1. 第 0 天初值作为个体锚点。
2. 乘法结构

\[
M_{i,s}(t)=M_{i,s}(0)\cdot R_s(t).
\]

3. “反推初值 / 时点等效观测判定线”只作为问题 3 的解释工具，不替代题目要求的动态阈值表。
4. block bootstrap + 三区域判定保留在问题 4 中。
5. 时间块交叉验证保留。
6. 环境项不作强因果外推的边界声明保留。

其中 \(R_s(t)\) 建议统一称为：

- 类型平均衰减函数；
- 或类型共享保留率函数。

不再使用“公共核”“统一退化机制”等过强表述。

---

## 3. 数据事实与结构发现

### 3.1 数据结构

题目材料包括：

- `B题 剩磁法雷击判定预测 问题.pdf`
- `附件1_模拟实验数据.csv`
- `附件2-weather_data.xlsx`

附件 1 字段为：

- 样品类型
- 编号
- 测量天数
- 剩磁(mT)
- 温度(℃)
- 相对湿度(%)

观测时间点为：

`0,1,2,\dots,15,22,30,37,44,51,58,90`

样品总数 60 个：

- 小号铁钉 20 个；
- 小号铁夹 20 个；
- 普通钢筋 10 个；
- 锈蚀钢筋 10 个。

### 3.2 第 0 天的正确定位

必须明确：

- 第 0 天在附件 1 中有完整记录；
- 第 0 天是雷击后初始剩磁基准点；
- 第 0 天观测值用于个体初值锚定；
- 环境累计暴露从第 1 天开始计算。

### 3.3 结构发现

对同类样品使用第 0 天做归一化：

\[
R_{i,s}(t)=\frac{M_{i,s}(t)}{M_{i,s}(0)}.
\]

数据表明，同类型样品的归一化曲线高度一致，因此可以将剩磁写成：

\[
M_{i,s}(t)=M_{i,s}(0)\cdot R_s(t).
\]

这表明：

- 个体差异主要体现在初始剩磁；
- 衰减规律主要由样品类型与环境共同决定；
- 这种乘法结构可以作为整篇论文的统一主线。

---

## 4. 问题 1：初值验证与 23-29 天预测

### 4.1 初值分两层表达

问题 1 中必须分个体层和类型层报告初值。

个体层：

- 直接使用每个样品的第 0 天实测值 \(M_{i,s}(0)\)。

类型层：

- 对四类样品分别给出第 0 天均值、标准差和 95% 置信区间。

当前数据的类型层统计量为：

| 样品类型 | 样本数 | 平均初值(mT) | 标准差 | 95%置信区间 |
|---|---:|---:|---:|---:|
| 小号铁钉 | 20 | 2.6269 | 0.6769 | [2.3101, 2.9438] |
| 小号铁夹 | 20 | 2.5214 | 0.4746 | [2.2992, 2.7435] |
| 普通钢筋 | 10 | 3.4839 | 1.0654 | [2.7219, 4.2460] |
| 锈蚀钢筋 | 10 | 4.1933 | 1.2647 | [3.2886, 5.0979] |

### 4.2 地基检验 1：初值与长期保留率相关性

对每类样品计算：

\[
\mathrm{corr}\Big(M_{i,s}(0),\frac{M_{i,s}(90)}{M_{i,s}(0)}\Big).
\]

论文中结论只写为：

> 当前数据未观察到明显强相关反证，支持“初值与类型平均衰减函数可近似分离”的建模假设。

不写“完全独立”。

### 4.3 地基检验 2：共享衰减函数合理性

比较：

- H0：同类样品共享类型平均衰减函数；
- H1：每个样品独立拟合归一化衰减曲线。

检验方式建议采用：

- AIC/BIC；
- 或 F 检验 / 似然比检验。

论文中必须给出检验表，而不能只写文字。

### 4.4 问题 1 的衰减模型

保留统一主线，但不再堆太多概念。建议主模型写为：

\[
\ln R_s(t)=-(a_st+\beta_sA_T(t)+\gamma_sA_H(t)+\delta_sA_{TH}(t)),
\]

于是：

\[
\hat M_{i,s}(t)=M_{i,s}(0)\cdot \hat R_s(t).
\]

其中：

\[
A_T(t)=\sum_{d=1}^{t}(T_d-\bar T),\qquad
A_H(t)=\sum_{d=1}^{t}(H_d-\bar H),
\]

\[
A_{TH}(t)=\sum_{d=1}^{t}\max(T_d-T^*,0)\max(H_d-H^*,0).
\]

这里的 \(A_{TH}(t)\) 作为门槛型环境交互项保留，它是本文最稳的“小创新点”。

### 4.5 第 23-29 天预测

第 23-29 天预测必须由统一模型直接给出：

\[
\hat M_{i,s}(t)=M_{i,s}(0)\cdot \hat R_s(t),\qquad t=23,\dots,29.
\]

对类型代表值：

\[
\hat M_{s,\mathrm{mean}}(t)=\bar M_s(0)\cdot \hat R_s(t).
\]

不能再改成插值。

---

## 5. 问题 2：独立贡献与交互效应分析

问题 2 必须回到题面要求，不能只做“模型误差比较”。正文必须回答：

- 时间主效应有多大；
- 温度主效应有多大；
- 湿度主效应有多大；
- 温度 × 湿度交互项是否显著；
- 时间 × 锈蚀交互项是否显著。

### 5.1 建议模型

建议建立规范的分层回归或混合效应模型。例如在归一化后，对数保留率建模：

\[
\ln R_{i,s}(t)
=
\alpha_0+\alpha_1 t+\alpha_2 T_t+\alpha_3 H_t+\alpha_4(T_tH_t)
+\alpha_5(t\cdot Rust_s)+u_s+\varepsilon_{i,s,t},
\]

其中：

- \(Rust_s\) 为锈蚀指示变量；
- \(u_s\) 表示类型层面的随机效应或固定分组效应。

如果直接使用累计量形式更稳定，也可以写成：

\[
\ln R_{i,s}(t)
=
\alpha_0+\alpha_1 t+\alpha_2 A_T(t)+\alpha_3 A_H(t)+\alpha_4A_{TH}(t)+\alpha_5(t\cdot Rust_s)+u_s+\varepsilon_{i,s,t}.
\]

### 5.2 必须报告的统计量

问题 2 主文必须报告：

- 回归系数；
- 标准误；
- t 值或 z 值；
- p 值；
- 偏 \(R^2\) 或其他效应量；
- 交互项方向与大小。

这样才算真正回答了“独立贡献与交互效应”。

### 5.3 消融实验作为补充证据

在完成上面的标准统计分析之后，再补一个工程化消融实验，比较：

- 无交互项；
- 对称交互项；
- 门槛型交互项。

补充报告：

- 分块 CV 误差；
- 锈蚀钢筋预测改善幅度。

主文本推荐口径：

> 为验证交互项的工程意义，进一步比较不同交互构造下的预测误差，发现门槛型交互在锈蚀钢筋上的预测表现更优。

这样就同时完成了题目要求的“归因”和你自己的“预测增益创新”。

### 5.4 问题 2 结论模板

建议写成：

1. 时间项反映基础自然退磁趋势；
2. 温度和湿度累计暴露对剩磁衰减具有显著修正作用；
3. 温度 × 湿度交互项在高温高湿区间内表现出更强的退磁放大效应；
4. 时间 × 锈蚀交互项表明锈蚀钢筋对长期衰减更敏感；
5. 由于所有样品共享同一条天气轨迹，环境项解释以统计关联和工程修正为主，不作强因果外推。

---

## 6. 问题 3：动态阈值修正表

问题 3 必须回到题目规定动作，明确给出 `1-90` 天动态阈值表。

### 6.1 动态阈值定义

对国标静态阈值 \(\Theta_s^{GB}\)，定义第 \(t\) 天动态判定阈值为：

\[
\Theta_s(t)=\Theta_s^{GB}\cdot R_s(t).
\]

其中国标阈值为：

\[
\Theta_s^{GB}=
\begin{cases}
1.0,& s\in\{\text{小号铁钉, 小号铁夹}\}\\
1.5,& s\in\{\text{普通钢筋, 锈蚀钢筋}\}
\end{cases}
\]

### 6.2 必须完成的输出

问题 3 正文必须明确写出：

- 依据衰减模型计算 1-90 天动态阈值；
- 将结果写入 Excel 表作为附件提交。

建议 Excel 列为：

- 天数；
- 天气；
- 气温；
- 湿度；
- 四类样品保留率 \(R_s(t)\)；
- 四类样品动态阈值 \(\Theta_s(t)\)；
- 四类样品类型均值预测剩磁。

### 6.3 反推初值作为补充解释

保留“反推初值”这一解释工具，但只作为问题 3 的补充角度：

\[
\hat M_0=\frac{M_{obs}}{R_s(t)}.
\]

并说明其与动态阈值表等价，只是另一种解释方式，而不是替代问题 3 的表格输出。

---

## 7. 问题 4：雷击判定流程与置信度评估

问题 4 必须按题目要求组织为四个环节。

### 7.1 数据预处理

补齐当前方案中明显缺失的预处理模块：

- 异常值检测；
- 缺失值填补；
- 单位检查；
- 样品类型核验；
- 延迟天数与天气轨迹匹配检查。

### 7.2 衰减预测

输入：

- 样品类型；
- 延迟天数；
- 天气轨迹；
- 实测剩磁。

输出：

- 预测保留率 \(R_s(t)\)；
- 或预测剩磁 \(\hat M_{i,s}(t)\)。

### 7.3 动态阈值调整

根据问题 3 的阈值表或公式计算：

\[
\Theta_s(t)=\Theta_s^{GB}\cdot R_s(t).
\]

### 7.4 结果置信度评估

采用按样品编号分层的 cluster block bootstrap，得到：

- 预测区间；
- 动态阈值区间；
- 反推初值区间。

在此基础上构建三区域判定：

- 支持：\(\hat M_0^L\ge \Theta_s^{GB}\)
- 不支持：\(\hat M_0^U< \Theta_s^{GB}\)
- 灰区：其余情况

同时给出概率辅助输出：

\[
P(\text{雷击})=
1-\Phi\left(
\frac{\Theta_s^{GB}-\hat M_0}{SE(\hat M_0)}
\right).
\]

### 7.5 长延迟场景的灰区规则

不再单独包装“证据寿命”概念，只保留工程化表达：

> 当延迟天数较大或区间相对宽度超过预设阈值时，判定自动进入灰区，建议结合其他证据或进行复检。

这里区间相对宽度定义为：

\[
\frac{\hat M_0^U-\hat M_0^L}{\hat M_0}.
\]

若超过阈值（如 100%），则进入长延迟失效区。

---

## 8. 参数估计、交叉验证与 Bootstrap

### 8.1 参数估计

建议采用：

- 主模型使用岭回归、GLS 或混合效应回归；
- 自变量先标准化后再估计；
- 时间与环境项共线明显，因此参数解释以稳健预测为主。

### 8.2 交叉验证

继续采用时间块交叉验证，防止信息泄漏。

### 8.3 Bootstrap

统一写成：

> 采用按样品编号分层的 cluster block bootstrap，在每次重抽样中保留同一样品的完整时间序列，并重新估计模型参数，以获得预测值、动态阈值和反推初值的区间估计。

并明确：

- 重抽样次数不少于 1000；
- 区间类型采用 percentile 或 BCa；
- 当前区间主要反映样品层不确定性，不完全覆盖天气跨年份外推风险。

---

## 9. 图表计划

建议最终稿至少补以下图表：

1. 四类样品原始剩磁衰减曲线；
2. 四类样品归一化曲线叠加图；
3. \(M_0\) 与 \(M_{90}/M_0\) 散点图；
4. 公共衰减函数检验结果表；
5. 含 / 不含交互项的 CV 误差对比图；
6. 四类样品动态阈值曲线图；
7. 反推初值区间宽度—延迟天数曲线图；
8. 现场判定流程图。

其中第 5、7、8 项最能提升论文层次。

---

## 10. 创新点写法

创新点统一写成以下四条：

1. 识别并验证了“个体初值 × 类型平均衰减函数”的乘法结构；
2. 在问题 3 中，将国标静态阈值映射为观测时点的动态判定线，并给出 1-90 天动态阈值表；
3. 在问题 2 中引入门槛型高温高湿交互项，并通过消融实验验证其对锈蚀钢筋预测的改善作用；
4. 在问题 4 中构建了基于 block bootstrap 的三区域工程判定流程。

这样的创新强度中等、证据充分，更符合评审口味。

---

## 11. 三句必须保留的边界声明

建议原样写入正文：

1. 本模型是基于实验数据与天气轨迹构建的经验衰减模型，而非完整磁畴微观机理模型。
2. 由于所有样品共享同一条天气轨迹，环境累计项主要用于描述该轨迹下衰减速度的经验修正，而不作严格独立因果解释。
3. 对长延迟、低剩磁及阈值附近样品，应采用三区域判定与复检建议，避免给出过度确定的结论。

---

## 12. 最终结论

最终正确的成稿方向不是继续拔高理论包装，而是回到：

> 一个完成度高、统计规范、工程清楚、带适度创新的竞赛建模方案。

因此，本版方案选择：

- 保留最强的结构发现；
- 删除支撑不足的强机理外衣；
- 四问逐条作答；
- 以门槛型环境交互与三区域判定作为主要亮点。

这一路线最符合竞赛评审逻辑，也最有利于稳定进入高分区。
